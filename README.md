# Choose Your Own Adventure

Simple, web-based "choose your own adventure" style stories from Markdown
files.

## Setup

Copy `example.html` to `public/index.html` and `example.md` to `public/content`.
Serve the `public` directory using a web server.
